name: Learn Github Actions

on:
  # push:
  #   branches: [main]
  
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a Greeting.
        run: echo "Hello, GitHub Actions! 🚀"
